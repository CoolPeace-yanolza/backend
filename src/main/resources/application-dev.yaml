spring:
  batch:
    jdbc:
      initialize-schema: always
    job:
      name: dailyStatisticsJob
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
logging:
  level:
    org.springframework.security: debug
